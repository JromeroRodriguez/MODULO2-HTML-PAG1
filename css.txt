* Reseteo básico para que no haya espacios extra */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    display: flex; /* Usamos flex para poner el aside y main lado a lado */
    flex-wrap: wrap; /* Permite que el header y la imagen hero ocupen todo el ancho */
}

/* El Header y la Imagen Hero ocupan el 100% arriba */
header, .img-container {
    width: 100%;
    padding: 20px;
    text-align: center;
}

header nav ul {
    list-style: none;
    display: flex;
    justify-content: center;
    gap: 20px;
}

/* Estilos de la imagen principal */
.img-container img {
    width: 100%;
    max-height: 400px;
    object-fit: cover;
}

/* --- DISEÑO DE COLUMNAS (Aside y Main) --- */

aside.contenedor-principal {
    width: 35%; /* Ancho de la columna izquierda */
    padding: 20px;
    background-color: #f9f9f9;
}

aside iframe {
    width: 100%; /* El mapa se ajusta al ancho del aside */
    height: 250px;
    margin-top: 10px;
}

main {
    width: 65%; /* Ancho de la columna derecha */
    padding: 20px;
}

/* Estilo para las 3 imágenes por estadio */
figure {
    display: grid;
    grid-template-columns: repeat(3, 1fr); /* Crea 3 columnas iguales */
    gap: 10px; /* Espacio entre imágenes */
}

figure img {
    width: 100%;
    height: 150px;
    object-fit: cover;
    border-radius: 8px;
}

/* Media Query: si se ve en celular, las columnas se ponen una arriba de otra */
@media (max-width: 768px) {
    aside.contenedor-principal, main {
        width: 100%;
    }
}